<UserControl x:Class="HomeCenter.NET.Controls.CommandControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="25" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="100*" />
            <ColumnDefinition Width="25" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        <Label Content="{Binding Path=Command.KeysString}" Grid.Column="0"/>
        <Label Content="{Binding Path=Command.Data}" Grid.Column="1"/>
        <Button Content="⚙" Grid.Column="2" Click="Edit_Click"/>
        <Button Content="✖" Grid.Column="3" Click="Delete_Click"/>
    </Grid>
</UserControl>
